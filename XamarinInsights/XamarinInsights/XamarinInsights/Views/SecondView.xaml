<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinInsights.Views.SecondView">
  <StackLayout>
    <Button
      Text="Event (Go Back)"
      Command="{Binding EventCommand}"/>
    <Button
      Text="Identity"
      BackgroundColor="Gray"
      Command="{Binding IdentityCommand}"/>
    <Button
      Text="Warning"
      BackgroundColor="Yellow"
      Command="{Binding WarningCommand}"/>
    <Button
      Text="Error"
      BackgroundColor="Red"
      Command="{Binding ErrorCommand}"/>
    <Button
      Text="Crash"
      BackgroundColor="Red"
      Command="{Binding ExceptionCommand}"/>
  </StackLayout>
</ContentPage>